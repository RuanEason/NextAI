{
  "name": "@copaw-next/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && pnpm exec tsc -p tsconfig.json && cp src/index.html dist/index.html && cp src/styles.css dist/styles.css",
    "test": "node -e \"console.log('web test placeholder')\"",
    "lint": "pnpm exec tsc -p tsconfig.json --noEmit"
  }
}
